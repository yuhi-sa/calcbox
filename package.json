{
  "name": "calcbox",
  "version": "1.0.0",
  "private": true,
  "description": "CalcBox - collection of useful calculation tools",
  "scripts": {
    "test": "node tests/run.js",
    "lint": "eslint js/*.js --no-error-on-unmatched-pattern",
    "minify": "node scripts/minify.js"
  },
  "devDependencies": {
    "eslint": "^8.57.0",
    "terser": "^5.31.0",
    "clean-css-cli": "^5.6.3"
  }
}
